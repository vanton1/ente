# CI irrelevant-path probe

This disposable file proves that fork CI returns stable green gates without
running expensive mobile or CodeQL validation for an unrelated Markdown-only
pull request. It is not intended for merge.
